define([

        // Core dependencies
        'jquery', 'lodash', 'box2d',

        // Application configuration
        'config'

    ], function(
        $, _, Box2D,
        config
    ) {

        /**
         * Function that initializes application
         */
        var init = function() {

        };

        /**
         * Initialization
         */
        $(function() {
            init();
        });
    }
);
