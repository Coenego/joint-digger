define([

        // Application configuration
        'config'

    ], function(config) {
        'use strict';

        /**
         * Constructor
         */
        function SoundController() {
            return this;
        };

        /**
         * Function that initializes the sound controller
         */
        SoundController.init = function() {

        };

        return SoundController;
    }
);
