define([

        // Application configuration
        'config'

    ], function(config) {
        'use strict';

        /**
         * Constructor
         */
        function Vehicle() {
            return this;
        };

        /**
         *
         */
        Vehicle.prototype.drive = function() {

        };

        return Vehicle;
    }
);
